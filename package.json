{
  "name": "@wgw-relay/monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "Open-core cellular messaging relay and connector protocol.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "node --test packages/protocol/test/*.test.js packages/server/test/*.test.js apps/hosted-console/test/*.test.js",
    "check": "node --check packages/protocol/src/index.js && node --check packages/server/src/relayService.js && node --check apps/hosted-console/src/store.js && node --check apps/hosted-console/src/app.js && node --check apps/hosted-console/src/server.js && node --check apps/hosted-console/public/app.js",
    "start:console": "npm --prefix apps/hosted-console start"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "Apache-2.0"
}
